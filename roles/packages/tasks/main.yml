#SPDX-License-Identifier: MIT-0
---
# tasks file for packages
- name:  install nginx
  ansible.builtin.apt:
    name: nginx
    state: absent
    update_cache: yes
  when: ansible_facts['os_family'] == 'Debian'
  tags:
    - packages
    - nginx
